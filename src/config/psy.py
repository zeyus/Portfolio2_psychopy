"""
Psychopy specific config
"""

PREFS = {
    # Hardware / display configuration
    # 'audioLib': ['PTB'],
    'winType': ['glfw'],
    # Setting to True makes text sharp, but causes problems with the dialogue
    'highDPI': False,
    'windowSize': (3200, 1800),
    'monitorName': 'Blade13scaled',
    'monitorWidth': 29.5,
    'fullScreen': True,

    # UI settings, colors, etc.
    'windowColor': (1, 1, 1),
    'textColor': (-1, -1, -1),
}
